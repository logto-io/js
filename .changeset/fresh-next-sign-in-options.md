---
"@logto/next": minor
---

support request-specific sign-in options

Add shared `signInOptions` defaults and a per-request `getSignInOptions` callback to Pages Router
auth routes. Use `firstScreen: 'register'` for the sign-up route.
